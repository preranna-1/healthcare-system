<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /healthcare-system/backend/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^api/([a-z_]+)/?(.*)$ api/$1.php?request=$2 [QSA,L]
</IfModule>
